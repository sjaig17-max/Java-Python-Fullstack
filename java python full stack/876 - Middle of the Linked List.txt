def middleNode(self, head: Optional[ListNode]) -> Optional[ListNode]:
	nodes = [] #array to keep track of the nodes
	while head:
		nodes.append(head)
		head = head.next
	middle = len(nodes)
	return nodes[middle] 